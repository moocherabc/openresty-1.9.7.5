
chmod +x configure  授权执行权限 再执行

我的编译参数

./configure --prefix=/data/services/openresty    --with-http_gzip_static_module  --with-http_realip_module --with-http_iconv_module --with-luajit --without-http_redis2_module  --with-http_stub_status_module


